serving:
  reverseProxyPrefix: www.ediblearrangements.com/stores
dynamicRoutes:
  - from: /assets/*
    to: /stores/assets/:splat
    status: 200
buildConfiguration:
  buildCommand: npm run build
  installDependenciesStep:
    command: npm install
    requiredFiles:
      - package.json
      - package-lock.json
      - .npmrc
livePreviewConfiguration:
  setupCommand: ":"
sitemap:
  excludeList:
    - edit