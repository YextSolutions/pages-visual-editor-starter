buildConfiguration:
  buildCommand: npm run build -- --reverse-proxy-prefix=$YEXT_BRANCH_REVERSE_PROXY_PREFIX
  installDependenciesStep:
    command: npm install
    requiredFiles:
      - package.json
      - package-lock.json
      - .npmrc
livePreviewConfiguration:
  setupCommand: ":"
sitemap:
  excludeList:
    - edit